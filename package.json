{
  "name": "consultaplaca",
  "version": "1.0.0",
  "description": "ConsultaPlaca - Raiz do projeto",
  "scripts": {
    "start": "node backend/src/server.js",
    "build": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
